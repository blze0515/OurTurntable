<?xml version="1.0" encoding="UTF-8" ?>

<!-- doctype mapper로 설정 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<!-- mapper namespace 지정 -->
<!-- 기존 JDBC 프로그래밍에서 DAO 클래스에 쿼리를 작성함 -->
<!-- DAO에서 쿼리가 분리되었기 때문에 DAO클래스 이름을 따라감 -->
<mapper namespace="MypageDAO">
	<!-- 쿼리 작성 영역 -->
	<select id="readFollowList" parameterType="string" resultType="com.spring.ott.VO.UserVO">
		SELECT 
		   A.RELATION_PARTNER ,
		   A.RELATION_DATE ,
		   B.USER_PROFILE_IMG 
		from 
		T_OTT_RELATION A,
		T_OTT_USER B,
		T_OTT_USER C
		WHERE A.RELATION_PARTNER = B.USER_ID 
		and C.USER_ID = #{userId}
		AND A.USER_ID = C.USER_ID
		and A.RELATION_CONDITION = 1
	</select>
	
	<select id="getBlockList" parameterType="string" resultType="com.spring.ott.VO.UserVO">
		SELECT 
		   A.RELATION_PARTNER ,
		   A.RELATION_DATE ,
		   B.USER_PROFILE_IMG 
		from 
		T_OTT_RELATION A,
		T_OTT_USER B,
		T_OTT_USER C
		WHERE A.RELATION_PARTNER = B.USER_ID 
		and C.USER_ID = #{userId}
		and A.RELATION_CONDITION = 2;
	</select>
	
	
</mapper>